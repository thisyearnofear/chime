services:
  chime:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:9127:3000"
    environment:
      - NEXT_PUBLIC_NETWORK=${NEXT_PUBLIC_NETWORK:-testnet}
      - NEXT_PUBLIC_BASE_URL=${NEXT_PUBLIC_BASE_URL:-http://localhost:9127}
      - VENICE_API_KEY=${VENICE_API_KEY:-}
      - FEATHERLESS_API_KEY=${FEATHERLESS_API_KEY:-}
    volumes:
      # Persistent chorus tallies + seat decisions survive restarts
      - chime-data:/app/.data

volumes:
  chime-data:
